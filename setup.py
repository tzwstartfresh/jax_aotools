"""Setuptools entry point for installing Jax-AOtools.

The project metadata is kept in ``pyproject.toml``. This file is provided for
users and tooling that still install packages with ``python setup.py``.
"""

from setuptools import setup


if __name__ == "__main__":
    setup()
